{
  "name": "dispatch",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dev:register": "tsx --env-file=.dev.vars src/register.ts",
    "prod:register": "tsx --env-file=.env src/register.ts",
    "dev:db": "wrangler d1 execute dispatch-db --local --file=db/schema.sql",
    "prod:db": "wrangler d1 execute dispatch-db --remote --file=db/schema.sql",
    "migrate:create": "wrangler d1 migrations create dispatch-db",
    "dev:migrate": "wrangler d1 migrations apply dispatch-db --local",
    "prod:migrate": "wrangler d1 migrations apply dispatch-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260723.1",
    "@types/node": "^26.1.1",
    "discord-api-types": "^0.38.51",
    "tsx": "^4.23.1",
    "typescript": "^5.5.2",
    "wrangler": "^4.113.0"
  },
  "dependencies": {
    "@discordjs/builders": "^1.14.1",
    "discord-hono": "^0.21.2"
  }
}
